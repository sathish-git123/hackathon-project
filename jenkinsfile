pipeline {
  agent any

  //tools {nodejs "NodeJS"}

  stages {
        stage('Checkout') {
            steps {
                checkout scm
            }
        }
        stage('Build') {
            steps {
                sh 'npm build'
            }s
        }

        stage('Test') {
            steps {
                sh 'npm test'
            }
        }
   }
}
